# L07 · 输出与安全（报告多格式 / Anthropic 兼容 / A-B Benchmark / 不可信输入防御）

> 面向：Java 后端 / 全栈工程师，把 skill-up 这个 Go 项目写进简历、在面试中讲清楚。
> 这是课程第 7 课，**把评测流水线末端「报告 / 量化 / 兼容」与贯穿始终的「安全护栏」一次讲透**。
> 模型搭对了（L01）、跑通了（L02）、懂主线（L03）和各子系统（L04–L06）后，这一课告诉你「结果怎么落盘、Skill 到底有没有用、为什么不会被 Custom Engine 反咬一口」。

---

## 1. 学习目标

学完本课，你应该能：

1. **报告多格式**：讲清 `JSONReporter / JUnitReporter / HTMLReporter` 的策略模式分工，以及「JSON 是事实源、JUnit/HTML 是派生视图」的 MVC 设计，并能说明 JUnit 三态（fail/error/skip）如何映射 CI 红绿。
2. **Anthropic 兼容**：说清 `grading.json` / `eval_metadata.json` / `benchmark.json` 三类产物的 schema 来源，以及 `ConvertToAnthropicGrading` 这种适配器为什么是「生态借力」而非「重复造轮子」。
3. **A/B Benchmark**：讲清 `with_skill vs without_skill` 的 4 指标（pass_rate / time / input_tokens / output_tokens）+ mean+stddev 怎么把「Skill 有效性」量化成 delta，并能对照 Java 圈的 A/B testing 数据管线。
4. **IterationWorkspace 目录治理**：能画出 Anthropic 兼容目录树，讲清 `validateCaseID` 三重拦截（`IsAbs` / `..` / 分隔符）+ `isRootedPath` 的 Windows 防御为什么是 OS 无关。
5. **安全防御全景**：能复述 Custom Engine 的 6 类威胁（路径穿越/TOCTOU、stale fixture、凭据泄漏、凭据进命令行/URL、SSRF、OOM），并针对每类讲出「攻击场景 → 防御代码 → Java 类比」，特别是 TOCTOU「check 挪到离 use 越近越好」的核心认知。

---

## 2. 本课在课程中的位置

| | |
|---|---|
| **前置** | [L06 跑得稳](./L06-跑得稳.md)（并发/重试/超时/Runtime 三后端）—— 你得先知道 case 是怎么跑完的，才能理解结果怎么落盘 |
| **定位** | 流水线末端（报告与量化）+ 贯穿始终（安全护栏） |
| **产出** | 一份能讲给别人听的「报告矩阵 + 安全威胁模型对照表」 |
| **不涉及** | 引擎适配细节（L05）、并发模型（L06）、面试口径（L08） |

> 顺序说明：L03–L06 都在讲「评测怎么跑」，本课讲「**跑完之后怎么办**」——
> 评测的产物（result.json / benchmark.json / report.html）是 skill-up 给用户的最终交付，而
> Custom Engine 这条不可信边界又是「敢不敢上生产」的关键。这两件事并在一起讲，是因为它们共同回答一个问题：
> **「这个工具产出的数据可信吗？这个工具本身安全吗？」**

整条流水线中的位置（来自 [`internal/runner/runner.go`](../../internal/runner/runner.go) 的包注释）：

```
Load Config → Validate → For each case:
  → Prepare Runtime
  → Install Skills
  → Provision MCP
  → Execute Engine
  → Judge Output
  → Collect Result
→ Generate Report   ← 报告层在这里
```

而**安全护栏**不是某一个阶段，它贯穿 Custom Engine 的整个生命周期（run 前 / run 中 / run 后），是「报告可信」的前提。

---

## 3. 讲解

### 3.1 Reporter 策略：JSON 事实源 / JUnit CI 拍板 / HTML 自包含

#### 3.1.1 接口定义（[`internal/report/reporter.go:12-14`](../../internal/report/reporter.go)）

```go
// Reporter writes evaluation output to a chosen format.
type Reporter interface {
    Write(ctx context.Context, in Input) error
}
```

只有 1 个方法、1 个入参 `Input`、1 个 error 返回。**Java 类比**：经典的策略模式 `Strategy` 接口，配合 `Context` 传递上下文。

```java
// Java 等价物
interface Reporter { void write(ReportInput in) throws IOException; }
```

差别只是 Go 把「异常」折叠成 `error` 返回值，不用 `throws` 声明。

#### 3.1.2 输入对象 `Input`（[`reporter.go:17-27`](../../internal/report/reporter.go)）

```go
type Input struct {
    SkillName     string           `json:"skill_name"`
    SchemaVersion string           `json:"schema_version"`
    EngineName    string           `json:"engine_name"`
    ModelName     string           `json:"model_name"`
    StartTime     time.Time        `json:"start_time"`
    EndTime       time.Time        `json:"end_time"`
    CaseResults   []CaseResult     `json:"case_results"`
    TotalTokens   int              `json:"total_tokens"`
    Benchmark     *BenchmarkResult `json:"benchmark,omitempty"`
}
```

注意 3 个细节：

- `Benchmark` 是指针 `*BenchmarkResult` + `omitempty`：没开 A/B 时 JSON 里**根本不出现**这个 key，而不是 `null`。这是 Go 处理「可选对象」的惯用法（Java 里类似 `Optional<BenchmarkResult>` 但语义更轻）。
- `Input` 字段挂了 `json` tag，**它本身就是 `JSONReporter` 直接 `json.MarshalIndent` 的产物**（[`json.go:20`](../../internal/report/json.go)）。这意味着 **`Input` 既是 Reporter 的输入，也是 JSON 的输出 schema**——零冗余。
- 两个派生方法 `TotalDuration()` / `OverallPassRate()`（[`reporter.go:31-54`](../../internal/report/reporter.go)）按需计算，且 `TotalDuration` 优先用 `StartTime/EndTime`，否则 fallback 到各 case duration 之和——fallback 是为了容错。

#### 3.1.3 三 Reporter 分工（一张表）

| Reporter | 文件 | 受众 | 角色 |
|---|---|---|---|
| `JSONReporter` | [`json.go:12-32`](../../internal/report/json.go) | 程序/下游脚本 | **事实源（fact source）**，所有视图的母版 |
| `JUnitReporter` | [`junit.go:18-40`](../../internal/report/junit.go) | CI / Jenkins / GitHub Actions | 通过 `<failure>` / `<error>` / `<skipped>` 拍板 CI 红/绿 |
| `HTMLReporter` | [`html.go:19-46`](../../internal/report/html.go) | 工程师肉眼 | 单页交互式报告（侧边栏导航 + 折叠 grading + baseline 对比） |

[`json.go:11`](../../internal/report/json.go) 注释直接挑明：

```go
// JSONReporter writes machine-readable JSON results.
// JSON is the "fact source" — JUnit and HTML are derived views.
```

**Java 类比**：MVC 里 `Model=JSON`、`View=JUnit/HTML`。JUnit 和 HTML 都从同一份 `Input` 派生，**谁也不存「私房数据」**，避免多源真相（multi-source-of-truth）。

#### 3.1.4 JUnit 三态映射：fail / error / skip（[`junit.go:117-154`](../../internal/report/junit.go)）

```go
switch cr.Status {
case judge.StatusFail:
    failures++
    tc.Failure = &junitFailure{
        Message: fmt.Sprintf("case %s failed", cr.CaseID),
        Type:    "AssertionFailure",
        Body:    buildFailureBody(cr),
    }
case judge.StatusError:
    errors++
    errMsg := cr.Error
    if errMsg == "" && cr.Grading != nil && cr.Grading.ErrorReason != nil {
        errMsg = *cr.Grading.ErrorReason
    }
    tc.Error = &junitError{Message: errMsg, Type: "ExecutionError", Body: errMsg}
case judge.StatusSkip:
    skipped++
    msg := ""
    if cr.Grading != nil && cr.Grading.SkipReason != nil {
        msg = *cr.Grading.SkipReason
    }
    tc.Skipped = &junitSkipped{Message: msg}
}
```

四态状态机（`PASS / FAIL / SKIP / ERROR`）映射到 JUnit 协议：

- `PASS`：什么都不挂，testcase 干净（CI 视为通过）。
- `FAIL`（**断言失败**）：业务上「答错了」，挂 `<failure type="AssertionFailure">`——CI 视为**红灯但构建成功**。
- `ERROR`（**执行异常**）：runtime 挂了（agent 进程崩溃），挂 `<error type="ExecutionError">`——CI 视为**构建失败**，比 failure 更严重。
- `SKIP`：主动跳过，挂 `<skipped>`——CI 视为**忽略**，不亮红。

> **JUnit 协议本身**就区分 `failures`（断言失败）和 `errors`（意外异常）。这是测试框架的共识——JUnit Java 原生也是 `AssertionFailedError` vs `RuntimeException` 的二元区分。skill-up 完整还原这套语义，**零信息损失**。

#### 3.1.5 HTML 工程亮点 1：`//go:embed` 自包含（[`html.go:304-314`](../../internal/report/html.go)）

```go
//go:embed templates/report.html
var htmlTemplate string

//go:embed templates/logo.png
var htmlLogoPNG []byte

// logoDataURI is the project logo encoded as a base64 data URI, embedded into
// the HTML report so the output file is self-contained and viewable offline.
var logoDataURI = template.URL("data:image/png;base64," + base64.StdEncoding.EncodeToString(htmlLogoPNG))
```

3 个关键点：

1. **`//go:embed`**：Go 1.16+ 的「编译期资源嵌入」。`templates/report.html` 和 `templates/logo.png` 在编译时被打进二进制，部署时**单文件可执行**。Java 等价物：Maven 的 `src/main/resources` + ClassLoader 加载，但 Go 这里更彻底，直接是字节数组。
2. **`var logoDataURI = ...`**：包级全局变量，**进程启动时计算一次**（PNG → base64 → data URI），后续每个 HTML 报告共享一份。
3. **`template.URL`**：绕过 Go `html/template` 的自动转义，因为 data URI 是给 `<img src="...">` 用的，自动转义会破坏 URL。注释 `//nolint:gochecknoglobals,gosec // G203` 说明 linter 被显式豁免——理由是「输入是编译期常量，不接受用户控制，没有 XSS 风险」。

#### 3.1.6 HTML 工程亮点 2：数据塞 `<script>` JSON（[`html.go:249-283`](../../internal/report/html.go)）

这是 HTML 报告最巧的设计——**Go 端不做 HTML 渲染，只把 JSON 塞进 `<script>` 标签，前端 JS 接管所有渲染**：

```go
ed := embeddedReportData{
    SkillName:   in.SkillName,
    // ... 字段映射 ...
    Cases:     cases,
    Benchmark: in.Benchmark,
}

jsonBytes, err := json.Marshal(ed)
if err != nil {
    return htmlReportData{}, fmt.Errorf("marshal embedded report data: %w", err)
}

return htmlReportData{
    SkillName:        in.SkillName,
    LogoDataURI:      logoDataURI,
    EmbeddedDataJSON: template.JS(jsonBytes), //nolint:gosec // trusted internal data, not user input
}, nil
```

- `embeddedReportData`（[`html.go:60-70`](../../internal/report/html.go)）是给前端用的「瘦过身」的数据模型——只挑前端要用的字段，是一种**显式的契约边界**。
- `template.JS(jsonBytes)`：把 JSON 字节流标记为「安全的 JS 字面量」，Go 模板会原样输出到 `<script>` 中，形如 `const DATA = {...};`。前端只需读 `DATA` 全局变量。

> **Java 类比**：Spring MVC 的 `@RestController` 返回 JSON + 前端 React/Vue 自己渲染。Go 这里是「服务端模板搭骨架（CSS、布局、logo）+ 客户端渲染数据」的混合模式（**Isomorphic-lite**），让单一 HTML 文件既有交互又自包含。把 `report.html` 邮件发给同事，断网环境打开都能看完整。

#### 3.1.7 管道友好的 `writeToOutput`（[`helpers.go:28-44`](../../internal/report/helpers.go)）

整个 report 包的「写入门面」，三个 Reporter 都从它走：

```go
func writeToOutput(path, desc string, fn func(w io.Writer) error) (err error) {
    if path == "" {
        return fn(os.Stdout)          // 空路径 → stdout（管道友好）
    }
    f, ferr := os.Create(path)
    if ferr != nil {
        return fmt.Errorf("create %s file: %w", desc, ferr)
    }
    defer func() {
        if cerr := f.Close(); cerr != nil && err == nil {
            err = fmt.Errorf("close %s file: %w", desc, cerr)
        }
    }()
    return fn(f)
}
```

逐行拆解：

1. **空路径写 stdout**：Unix 管道哲学——没指定 `-o report.json` 时，结果直接打到 stdout，可以被 `| jq` 或重定向消费。调用方传一个 `func(w io.Writer)`，对文件和 stdout 完全无感。
2. **命名的返回值 `err`**：配合 `defer` 里「覆盖返回值」——如果 `fn` 已经返回了非空 error，`Close` 错误就**不要覆盖**（`err == nil` 时才赋值），避免「关闭失败把真正的业务错误吞掉」。
3. **`defer` 关闭文件**：Go 处理资源释放的标准范式。Java 等价于 `try-with-resources`，但 Go 没有 ARM 块，只能 `defer`。
4. **`%w` 包装错误**：错误信息层层包装但保留原始 chain，可用 `errors.Unwrap` 还原。Java 类似 `new IOException("...", cause)`。

---

### 3.2 Anthropic 兼容：为什么「兼容 = 生态借力」

#### 3.2.1 三种 Anthropic 兼容产物

| 文件 | 位置 | Go 类型 | 写入函数 |
|---|---|---|---|
| `grading.json` | `<case>/<config>/grading.json` | `AnthropicGrading`（[`grading.go:26-30`](../../internal/report/grading.go)） | `WriteGradingJSON`（[`grading.go:86-88`](../../internal/report/grading.go)） |
| `eval_metadata.json` | `<case>/eval_metadata.json` | `EvalMetadata`（[`grading.go:104-109`](../../internal/report/grading.go)） | `WriteEvalMetadata`（[`grading.go:113-115`](../../internal/report/grading.go)） |
| `benchmark.json` | `iteration-N/benchmark.json` | `AnthropicBenchmark`（[`benchmark_anthropic.go:22-27`](../../internal/report/benchmark_anthropic.go)） | `WriteAnthropicBenchmark`（[`benchmark_anthropic.go:218-220`](../../internal/report/benchmark_anthropic.go)） |

#### 3.2.2 `AnthropicGrading` schema（[`grading.go:18-45`](../../internal/report/grading.go)）

```go
type AnthropicGrading struct {
    Expectations []AnthropicExpectation `json:"expectations"`
    Summary      AnthropicSummary       `json:"summary"`
    JudgeContext *judge.ContextMetadata `json:"judge_context,omitempty"`
}

type AnthropicExpectation struct {
    Text     string `json:"text"`
    Passed   bool   `json:"passed"`
    Evidence string `json:"evidence"`
}

type AnthropicSummary struct {
    Passed   int     `json:"passed"`
    Failed   int     `json:"failed"`
    Total    int     `json:"total"`
    PassRate float64 `json:"pass_rate"`
}
```

字段命名是关键——「expectations / passed / evidence / pass_rate」这些**精确对齐** Anthropic skill-creator 的输出 schema。文件头注释（[`grading.go:1-6`](../../internal/report/grading.go)）直接挑明：「enabling interoperability with eval-viewer and other Anthropic tooling」。

#### 3.2.3 适配器 `ConvertToAnthropicGrading`（[`grading.go:47-82`](../../internal/report/grading.go)）

```go
func ConvertToAnthropicGrading(result *judge.Result) *AnthropicGrading {
    if result == nil {
        return &AnthropicGrading{
            Expectations: []AnthropicExpectation{},
            Summary:      AnthropicSummary{},
        }
    }

    expectations := make([]AnthropicExpectation, 0, len(result.AssertionResults))
    for _, ar := range result.AssertionResults {
        expectations = append(expectations, AnthropicExpectation{
            Text:     ar.Text,
            Passed:   ar.Passed,
            Evidence: ar.Evidence,
        })
    }

    return &AnthropicGrading{
        Expectations: expectations,
        Summary:      AnthropicSummary{
            Passed:   result.Summary.Passed,
            Failed:   result.Summary.Failed,
            Total:    result.Summary.Total,
            PassRate: result.Summary.PassRate,
        },
        JudgeContext: result.JudgeContext,
    }
}
```

经典的**适配器模式（Adapter Pattern）**——`judge.Result` 是内部模型，`AnthropicGrading` 是外部 schema，转换函数负责字段级映射。3 个细节：

1. **`nil` 安全**（`result == nil`）：上游忘填 grading 时，返回空结构（`Expectations: []` 非_nil 空切片），保证 JSON 输出 `"expectations": []` 而非 `"expectations": null`。Anthropic 工具解析时空数组比 null 更友好。
2. **预分配容量** `make([]..., 0, len(...))`：避免 append 时的多次扩容拷贝。
3. **`JudgeContext` 透传**：这是 skill-up 在 Anthropic schema 上的**扩展字段**——`omitempty` 意味着 Anthropic eval-viewer 会忽略这个字段，但 skill-up 自己的工具能消费。这是「兼容 + 扩展」的平衡。

> **Java 类比**：MapStruct 的 `@Mapper` 自动生成的字段映射代码。Go 没有 MapStruct，但这种 1:1 字段映射的样板代码很短（10 行），手写比引入框架更划算。

#### 3.2.4 为什么「兼容 = 生态借力」

如果 skill-up 自己发明一套报告 schema（比如叫 `skill-up-result.json`），用户拿到产物只能用 skill-up 自己的工具看，**生态被锁死**。而兼容 Anthropic schema 意味着：

- 用户已有 Anthropic eval-viewer / CI 集成 → 直接复用，**迁移成本为零**；
- Anthropic 未来更新 schema → skill-up 跟着升级适配器即可，**不破坏前端工具链**；
- skill-up 在 Anthropic 生态里能直接被讨论、对比、引用。

> 这是开源项目**进入主流生态**的关键策略——**「少发明轮子，多对齐标准」**。
> Java 类比：Spring Data 对齐 JPA 标准、Spring Boot Starter 对齐 MicroProfile；Apache 项目几乎都对齐各自的工业标准。

---

### 3.3 Benchmark A/B：把 Skill 有效性量化

Benchmark 是评测的「**结论性数字**」：开 Skill 比 baseline 到底好多少？

#### 3.3.1 两套 Benchmark：内部 vs Anthropic

skill-up 有两套 Benchmark 数据结构：

- **内部 Benchmark**（[`benchmark.go`](../../internal/report/benchmark.go) + [`reporter.go:83-115`](../../internal/report/reporter.go)）：用于 `result.json` 内嵌的 `benchmark` 字段。
- **Anthropic Benchmark**（[`benchmark_anthropic.go`](../../internal/report/benchmark_anthropic.go)）：用于独立 `benchmark.json` 文件，schema 完全对齐 Anthropic，多 min/max + 字符串格式化 delta。

> 为什么两套？因为 `result.json` 内嵌版本面向程序消费（数字精确），Anthropic 版本面向 eval-viewer（schema 兼容 + min/max + 字符串 delta 便于直接渲染）。

#### 3.3.2 3 个统计基础函数（[`benchmark.go:32-72`](../../internal/report/benchmark.go)）

```go
// mean computes the arithmetic mean of a float64 slice.
func mean(values []float64) float64 {
    if len(values) == 0 {
        return 0
    }
    var sum float64
    for _, v := range values {
        sum += v
    }
    return sum / float64(len(values))
}

// stdDev computes the population standard deviation of a float64 slice.
func stdDev(values []float64) float64 {
    n := len(values)
    if n < 2 {
        return 0
    }
    avg := mean(values)
    var sumSq float64
    for _, v := range values {
        d := v - avg
        sumSq += d * d
    }
    return math.Sqrt(sumSq / float64(n))
}
```

3 个工程细节：

1. **总体标准差**（除以 `n`，不是 `n-1`）：注释 `population standard deviation`。这是统计学上的取舍——我们关注的是**这 N 次运行本身**的离散度（描述统计），而不是**估计总体**（推断统计）。Java 类似 Apache Commons Math 的 `StandardDeviation` 默认是总体版本。
2. **`n < 2` 返回 0**：单点没法算方差。
3. **空切片返回 0** 而非 panic：函数式友好的容错。

#### 3.3.3 `ComputeBenchmarkStats` 4 指标（[`benchmark.go:79-109`](../../internal/report/benchmark.go)）

```go
func ComputeBenchmarkStats(metrics []CaseMetrics) BenchmarkStats {
    // ...
    passFloats := make([]float64, len(passed))
    for i, p := range passed {
        if p {
            passFloats[i] = 1
        }
    }

    return BenchmarkStats{
        PassRate:     StatValue{Mean: passRate(passed), StdDev: stdDev(passFloats)},
        TimeSeconds:  StatValue{Mean: mean(times), StdDev: stdDev(times)},
        InputTokens:  StatValue{Mean: mean(inputTokens), StdDev: stdDev(inputTokens)},
        OutputTokens: StatValue{Mean: mean(outputTokens), StdDev: stdDev(outputTokens)},
    }
}
```

**4 个指标**回答了 Skill 评测最关心的 4 个问题：

| 指标 | 业务含义 |
|---|---|
| `pass_rate` | **质量**：Skill 有没有提升任务完成率？ |
| `time_seconds` | **延迟代价**：Skill 加进去后变快还是变慢？ |
| `input_tokens` | **prompt 成本**：Skill 注入的上下文贵不贵？ |
| `output_tokens` | **响应膨胀**：Skill 有没有诱导模型啰嗦？ |

**关键巧思**（[`benchmark.go:96-101`](../../internal/report/benchmark.go)）：`pass_rate` 的 stddev 用的是「布尔转 0/1 浮点数」再算 stddev——这等价于**伯努利分布的标准差** `sqrt(p*(1-p))`。这种「借用数值统计学公式表达分类变量离散度」的小技巧避免了单独写一份伯努利 stddev 函数。

#### 3.3.4 `ComputeBenchmarkDelta`：减法（[`benchmark.go:112-119`](../../internal/report/benchmark.go)）

```go
func ComputeBenchmarkDelta(withSkill, withoutSkill BenchmarkStats) BenchmarkDelta {
    return BenchmarkDelta{
        PassRate:     withSkill.PassRate.Mean - withoutSkill.PassRate.Mean,
        TimeSeconds:  withSkill.TimeSeconds.Mean - withoutSkill.TimeSeconds.Mean,
        InputTokens:  withSkill.InputTokens.Mean - withoutSkill.InputTokens.Mean,
        OutputTokens: withSkill.OutputTokens.Mean - withoutSkill.OutputTokens.Mean,
    }
}
```

delta 是「减法」——`with_skill.mean - without_skill.mean`。注意：**只用 mean，不用 stddev**——delta 本身不带置信区间，这是「描述性差异」，不是「统计显著性检验」。生产级 A/B 一般还要做 t-test，但 skill-up 选择了「够用即可」。

#### 3.3.5 简化模式 vs 完整模式（[`benchmark.go:121-144`](../../internal/report/benchmark.go)）

```go
func ComputeBenchmark(withSkillMetrics []CaseMetrics, withoutSkillMetrics []CaseMetrics) *BenchmarkResult {
    withStats := ComputeBenchmarkStats(withSkillMetrics)

    result := &BenchmarkResult{
        RunSummary: BenchmarkRunSummary{
            WithSkill:    withStats,
            WithoutSkill: nil,
            Delta:        nil,
        },
    }

    if withoutSkillMetrics != nil {
        withoutStats := ComputeBenchmarkStats(withoutSkillMetrics)
        delta := ComputeBenchmarkDelta(withStats, withoutStats)
        result.RunSummary.WithoutSkill = &withoutStats
        result.RunSummary.Delta = &delta
    }

    return result
}
```

两种模式：

- **简化模式**（默认）：`withoutSkillMetrics == nil` → `WithoutSkill` 和 `Delta` 都是 `nil`，JSON 输出为 `null`。
- **完整模式**（`benchmark.enabled=true`）：传入 baseline metrics，计算 `withoutStats` 和 `delta`，挂上指针。

> **Java 类比**：策略模式 + 空对象模式。`null` 在 Java 里很危险，但 Go 配合 `omitempty` + 指针 + nil check 是一种受控的「可选性」表达。
>
> **业务意义**：`with_skill - without_skill` 的 delta 回答「**这个 Skill 值得上线吗**」；stddev 回答「**结果稳吗**」。没有这套量化，skill-up 只是个「跑用例」工具；有了它，skill-up 是个「**Skill ROI 评估平台**」。

#### 3.3.6 Anthropic 版：Functional Options（[`benchmark_anthropic.go:137-199`](../../internal/report/benchmark_anthropic.go)）

```go
func ComputeAnthropicBenchmark(
    skillName, skillPath string,
    withSkillRuns []BenchmarkRun,
    withoutSkillRuns []BenchmarkRun,
    opts ...func(*benchmarkOptions),
) *AnthropicBenchmark {
    o := benchmarkOptions{timestamp: time.Now().UTC()}
    for _, fn := range opts {
        fn(&o)
    }
    timestamp := o.timestamp.Format(time.RFC3339)
    // ...
}

func WithTimestamp(t time.Time) func(*benchmarkOptions) {
    return func(o *benchmarkOptions) {
        o.timestamp = t
    }
}
```

Go 的 **Functional Options 模式**（Dave Cheney 推广）：

- 必填参数（skillName/skillPath/runs）走位置参数；
- 可选参数（timestamp）走 `opts ...func(*T)` 变长参数；
- 调用方：`ComputeAnthropicBenchmark(name, path, with, nil, report.WithTimestamp(t))` 或不传最后一个 → 用默认 `time.Now().UTC()`。

> **Java 类比**：Builder 模式（`new Builder().withTimestamp(t).build()`）。Go 没有 builder 语法糖，但 Functional Options 达到了同样的「可选 + 链式 + 默认值」效果，且**测试友好**——测试可注入固定 timestamp 让输出可断言。未来加 `WithNote()`、`WithRunsPerConfiguration()` 都不需要改函数签名。

#### 3.3.7 Anthropic Delta：字符串格式化（[`benchmark_anthropic.go:179-183`](../../internal/report/benchmark_anthropic.go)）

```go
bm.RunSummary.Delta = &AnthropicDelta{
    PassRate:    fmt.Sprintf("%+.2f", withSummary.PassRate.Mean-withoutSummary.PassRate.Mean),
    TimeSeconds: fmt.Sprintf("%+.1f", withSummary.TimeSeconds.Mean-withoutSummary.TimeSeconds.Mean),
    Tokens:      fmt.Sprintf("%+.1f", withSummary.Tokens.Mean-withoutSummary.Tokens.Mean),
}
```

- `%+` 中的 `+` 表示**强制输出正负号**——`+5.20` 和 `-3.10`，让人一眼看出方向。
- 不同精度（PassRate 用 `.2` 即 2 位小数，Time/Tokens 用 `.1` 即 1 位）——根据「业务关心的小数位」调整。
- Delta 是 `string` 而非 `float64`：因为 delta 要带符号显示，用字符串预格式化避免前端再处理。

---

### 3.4 IterationWorkspace 目录治理

[`IterationWorkspace`](../../internal/report/workspace.go) 是 skill-up 落盘产物的**目录治理者**，把所有 case 产物组织成 Anthropic 兼容的目录树。

#### 3.4.1 目录结构图（[`workspace.go:1-21`](../../internal/report/workspace.go) 文件头注释）

```
<skill-name>-workspace/
  iteration-<N>/
    benchmark.json
    benchmark.md
    report.html
    result.json
    report.xml           (可选, --format junit)
    report.json          (可选, --format json)
    <case-id>/
      eval_metadata.json
      with_skill/
        outputs/
          response.md
          grading.json
      without_skill/          # 可选, 仅当 benchmark.enabled=true
        outputs/
          response.md
          grading.json
```

设计要点：

- **workspace 与 skill 目录平级**：`<skill>/` 和 `<skill>-workspace/` 是兄弟（runner 明确："workspace sits alongside the skill directory"）——这是 Anthropic eval layout 的约定，便于将整个 workspace 一起 zip 分享。
- **iteration-N 是版本号**：每次评测递增（`nextIterationNumber` 扫描 `iteration-N` 目录取 max+1，见 [`runner.go:493-511`](../../internal/runner/runner.go)）。多轮迭代可以对比「v1 的 Skill vs v2 的 Skill」。
- **case 维度子目录**：每个 case 一个目录，互不干扰。
- **with_skill/without_skill** 双分支：仅当 `benchmark.enabled=true` 时才创建 `without_skill` 分支（节省磁盘 + 表达「是否做 A/B」）。

#### 3.4.2 `IterationWorkspace` 结构（[`workspace.go:54-86`](../../internal/report/workspace.go)）

```go
type IterationWorkspace struct {
    RootDir      string
    IterationNum int
    SkillName    string
}

func NewIterationWorkspace(outputDir, skillName string, iterNum int) (*IterationWorkspace, error) {
    if outputDir == "" {
        outputDir = skillName + "-workspace"
    }
    if iterNum < 1 {
        return nil, fmt.Errorf("iteration number must be >= 1, got %d", iterNum)
    }
    if err := os.MkdirAll(outputDir, dirPerm); err != nil {
        return nil, fmt.Errorf("create workspace root %s: %w", outputDir, err)
    }
    return &IterationWorkspace{
        RootDir:      outputDir,
        IterationNum: iterNum,
        SkillName:    skillName,
    }, nil
}
```

3 个不变量：

1. **`outputDir` 空时默认 `<skill>-workspace`**：默认行为统一。
2. **`iterNum < 1` 拒绝**：1-based 索引，避免 `iteration-0` 这种容易混淆的目录（对齐 Anthropic 习惯，也符合自然语言「第 1 次」）。
3. **构造时即创建 root**：`MkdirAll` 幂等，存在不报错。

权限常量（[`workspace.go:30-33`](../../internal/report/workspace.go)）：`dirPerm = 0o755`、`filePerm = 0o644`——Unix 标准（owner 可写，其他只读）。Go 的 `0o` 前缀是 Go 1.13+ 的八进制字面量，比 `0755` 更醒目。

#### 3.4.3 `validateCaseID` 防穿越（[`workspace.go:45-51`](../../internal/report/workspace.go)）

```go
// validateCaseID checks that caseID does not contain path traversal sequences.
func validateCaseID(caseID string) error {
    cleaned := filepath.Clean(caseID)
    if filepath.IsAbs(cleaned) || strings.Contains(cleaned, "..") || strings.ContainsAny(cleaned, `/\`) {
        return fmt.Errorf("invalid case ID: %q", caseID)
    }
    return nil
}
```

3 重拦截：

1. **`filepath.IsAbs`**：拒绝绝对路径（如 `/etc/passwd` 或 `C:\Windows`）。
2. **`strings.Contains(cleaned, "..")`**：拒绝「../」穿越（即使 Clean 之后还残留，比如 `foo/../etc`）。
3. **`ContainsAny(cleaned, `/\`)`**：拒绝**任何**分隔符——意味着 case ID 只能是单层目录名，不能是 `sub/foo`。

**为什么这么严？** 因为 case ID 来自 YAML 配置文件（`evals[].id`），是**用户输入**。如果用户写 `id: ../../../etc/passwd`，写入 grading.json 时会逃出 workspace 边界——这是经典的**路径穿越攻击（Path Traversal）**。

#### 3.4.4 `isRootedPath`：Windows 防御（[`workspace.go:33-42`](../../internal/report/workspace.go)）

```go
// isRootedPath reports whether p begins with either path separator. On
// Windows filepath.IsAbs requires a volume name (e.g. `C:\…`), so a POSIX-
// style "/abs.txt" passed in from user-supplied YAML would otherwise be
// accepted as a relative path; treat any leading `/` or `\` as rooted to
// keep the path-traversal guard OS-independent.
func isRootedPath(p string) bool {
    return strings.HasPrefix(p, "/") || strings.HasPrefix(p, `\`)
}
```

注释非常清楚地解释了动机：**`filepath.IsAbs` 在 Windows 上要求带盘符**，所以 `/etc/passwd` 在 Windows 上会被误判为「相对路径」！如果只依赖 `IsAbs`，攻击者用 POSIX 风格路径就能绕过。这里**额外**用 `isRootedPath` 拦截「任何以 `/` 或 `\` 开头的路径」，做到 OS 无关。

这是 `WriteFile`（[`workspace.go:215-234`](../../internal/report/workspace.go)）的防御：

```go
func (w *IterationWorkspace) WriteFile(relPath string, data []byte) error {
    cleaned := filepath.Clean(relPath)
    if filepath.IsAbs(cleaned) || isRootedPath(cleaned) || strings.HasPrefix(cleaned, "..") {
        return fmt.Errorf("invalid relative path: %s", relPath)
    }
    path := filepath.Join(w.IterationDir(), cleaned)
    // ...
}
```

> **Java 类比**：Spring 的 `Paths.get(base).resolve(userInput).normalize()` + 检查 `startsWith(base)`。OWASP 推荐的「规范化后比对前缀」策略。Go 这里更严，直接禁止所有分隔符。

#### 3.4.5 自适应调度：`ensureCaseDirs`（[`runner.go:268-279`](../../internal/runner/runner.go)）

```go
func ensureCaseDirs(ws *report.IterationWorkspace, results []evaluator.EvalResult, caseIDs []string) error {
    var err error
    if hasBaselineConfiguration(results) {
        err = ws.EnsureDirsWithBaseline(caseIDs)
    } else {
        err = ws.EnsureDirs(caseIDs)
    }
    // ...
}
```

runner 不写死「用哪个」，而是**根据 results 里有没有 `without_skill` 配置动态选择**。这种「数据驱动布局」的好处是：评测逻辑改了（开启/关闭 benchmark），目录布局自动跟上，零代码改动。

---

### 3.5 Runner.WriteResults 编排（[`runner.go:191-238`](../../internal/runner/runner.go)）

把上面所有组件串起来的总入口。**完整时序**：

```
WriteResults(results, formats)
  │
  ├─ indexCaseResults         ──→ caseIDs (有序去重)
  ├─ ensureCaseDirs           ──→ 创建目录树 (with/without 自适应)
  ├─ groupResultsByCase       ──→ 按 case 分组
  │
  ├─ writeGroupedCaseArtifacts
  │     ├─ writeCaseArtifacts (per case × per config)
  │     │     ├─ WriteResponse     → response.md
  │     │     ├─ WriteGrading      → grading.json  (Anthropic 兼容)
  │     │     └─ WriteEvalMeta     → eval_metadata.json
  │     └─ resultToBenchmarkRun    → 累积 with/without runs
  │
  ├─ ComputeAnthropicBenchmark ──→ benchmark.json schema
  ├─ WriteBenchmark / WriteBenchmarkMD  → benchmark.{json,md}
  │
  ├─ buildReportInput          ──→ report.Input (含 Cases, TotalTokens)
  ├─ ws.WriteFile("result.json")  ──→ 总事实源
  │
  ├─ writeFormattedReports     ──→ report.{json,xml,html} (按 formats)
  │     ├─ JSONReporter.Write
  │     ├─ JUnitReporter.Write
  │     └─ HTMLReporter.Write
  │
  └─ ui.Step / ui.Statusf      ──→ CLI 反馈
```

`writeFormattedReports`（[`runner.go:323-344`](../../internal/runner/runner.go)）就是**工厂方法模式**的轻量版——根据 format 字符串选择具体 Reporter：

```go
for _, format := range formats {
    switch format {
    case "json":
        reporter := &report.JSONReporter{OutputPath: filepath.Join(iterDir, "report.json")}
        // ...
    case "junit":
        reporter := &report.JUnitReporter{OutputPath: filepath.Join(iterDir, "report.xml")}
        // ...
    case "html":
        reporter := &report.HTMLReporter{OutputPath: filepath.Join(iterDir, "report.html")}
        // ...
    }
}
```

> **新增格式（如 Sarif / CSV）只需**：写一个新 struct 实现 `Reporter`，在 switch 里加一个 case。**不动现有代码**——这是开闭原则（OCP）的落地。

---

### 3.6 安全威胁模型（Custom Engine 不可信输入）

> 这一节是本课的重头戏。**Custom Engine = 用户用 YAML 声明的一个外部 Agent（一条 shell 命令 / 一个 HTTP 服务）**，skill-up 把 case 输入交给它、把它吐回的 JSON/文件当作 `SessionResult` 去打分。它在数据流上处于**完全不可信**的位置。

为什么不可信？看它返回的结构（[`internal/agent/custom.go:480-492`](../../internal/agent/custom.go)）：

```go
type parsedSessionResult struct {
    Engine       string                `json:"engine"`
    Model        string                `json:"model"`
    ExitCode     *int                  `json:"exit_code"`
    FinalMessage string                `json:"final_message"`
    Stderr       string                `json:"stderr"`
    Transcript   transcript.Transcript `json:"transcript"`
    Artifacts    *SessionArtifacts     `json:"artifacts"`
    // ...
}
```

**每一行都是攻击面**：

- `engine` / `model` —— 可能 echo 回配置的 API key（skill-up 会把它写进 `result.json` 和 OTel span）；
- `final_message` / `stderr` / `transcript` —— 同上，且会**流给 Judge LLM** 和 CI 日志；
- `artifacts.files[].path` —— 可能是 `/etc/passwd` 或 `../../../.ssh/id_rsa`，让 skill-up 把宿主机文件拷进报告目录（**路径穿越**）；
- `artifacts.files[].url` —— 可能指向 `http://169.254.169.254/...`（云元数据服务，**SSRF**）或一个会 302 跳到内网的入口；
- 一个巨大的 inline `content_base64` 或一个无限的 HTTP 响应体 —— **内存耗尽**（OOM）。

而本地传输（`transport: local`）更凶：skill-up 要按它声明的 `command` 在宿主机 / 容器里起进程，命令行如果包含 API key，会**直接出现在 `ps`、exec span、shell 历史**里。

> **Java 类比**：这就像你写了一个接收第三方回调的 Webhook 服务，对方的 JSON 体里任何一个字段都可能带攻击载荷。差别是，Webhook 还能躲在 HTTPS 后面，Custom Engine 是**本地起进程 + 读文件 + 发 HTTP**，攻击面是三维的。

#### 3.6.1 威胁清单（6+2 类）

| # | 威胁 | 攻击场景 | 受害资产 | 防御位置 |
|---|------|----------|----------|----------|
| T1 | **路径穿越 / 符号链接 TOCTOU** | engine 返回 `path: "/etc/passwd"` 或在 workspace 里种一个指向 `~/.ssh` 的符号链接 | 宿主机敏感文件外泄 | §3.6.2 `workspacePath` |
| T2 | **stale fixture 误读** | 上次跑残留的 `outputs/result.json` 被当成这次的成功结果 | 评测结果失真 | §3.6.3 `clearStaleOutputFile` |
| T3 | **凭据泄漏到日志/报告/Judge** | engine 把 `$ANTHROPIC_API_KEY` 原样 echo 进 `final_message` | API key 泄漏 | §3.6.4 `maskAPIKey` |
| T4 | **凭据进命令行 / URL** | 用户把 `${api_key}` 写进 `local.command` 或 `http.url` | API key 进 `ps` / proxy 日志 | §3.6.5 `containsAPIKey` |
| T5 | **SSRF（artifact 重定向到内网）** | engine 返回 `url: http://internal-metadata/...`，下载器跟随 302 跳到云元数据服务 | 内网探测 / 凭据窃取 | §3.6.6 不跟随 3xx |
| T6 | **内存耗尽** | engine 返回 800MB 的 inline `content_base64`，或 HTTP 服务回一个无上限的流 | DoS | §3.6.7 size cap |
| T7 | **凭证写进会被日志暴露的字段** | 用户把 `OPENAI_API_KEY=sk-real-key` 直接写进 `args` | API key 进命令行 | §3.6.8 `resolveEnvRefsStrict` |
| T8 | **间接走私** | 用 `${WRAPPER}` 包装一个敏感名，绕过单次检查 | API key 进命令行 | §3.6.8 迭代展开 |

---

#### 3.6.2 防御 T1：`workspacePath` 路径穿越 + 符号链接 TOCTOU

这是整篇文档最硬核的一段，因为 TOCTOU（Time-of-Check to Time-of-Use）是少数**只能靠「在 use 时刻重新 check」才能堵住**的漏洞类别——一次检查不够。

##### 攻击场景（3 种）

1. **静态穿越**：engine 返回 `path: "../../../etc/passwd"`，归档器 `filepath.Join(workspace, path)` 后拼出一个 workspace 外的路径，把 `/etc/passwd` 拷进报告目录。
2. **符号链接外指**：engine 在 workspace 里 `ln -s /root/.ssh id_rsa`，然后返回 `path: "id_rsa"`。`filepath.Clean` 看不出问题（路径就是 `id_rsa`），但 `read` 会跟随链接读到 `/root/.ssh/id_rsa`。
3. **TOCTOU**：即使你在 use 之前 `EvalSymlinks` 检查过 `outputs/newdir/result.json` 是干净的，engine 在你**检查完到真正 `rm` / `read` 之间**把 `outputs/newdir` 创建成指向 `/tmp` 的符号链接，你的 use 就跟随了被种的链接——这就是经典的 check-then-use 竞态。

##### 防御代码（[`internal/agent/custom.go:1115-1149`](../../internal/agent/custom.go)）

```go
// workspacePath resolves a path against the runtime workspace and confines it
// to that workspace. ...
func workspacePath(rt Runtime, p string) (string, error) {
    if p == "" {
        return p, nil
    }
    // (1) 先把 workspace 根本身用 EvalSymlinks 解析 —— 防止 workspace 本身就是一个软链
    ws, err := filepath.EvalSymlinks(filepath.Clean(rt.Workspace()))
    if err != nil {
        // Workspace must exist; if EvalSymlinks fails fall back to the
        // lexical root so we still reject obvious escapes.
        ws = filepath.Clean(rt.Workspace())
    }
    var abs string
    if filepath.IsAbs(p) {
        abs = filepath.Clean(p)
    } else {
        abs = filepath.Clean(filepath.Join(ws, p))   // (2) 相对路径 join 到 workspace
    }
    // (3) 解析「最深的存在祖先」的符号链接，把中间目录里藏的软链也展平
    resolved := resolveExistingPrefix(abs)
    rel, err := filepath.Rel(ws, resolved)
    if err != nil || rel == ".." || strings.HasPrefix(rel, ".."+string(filepath.Separator)) {
        return "", fmt.Errorf("path %q escapes the runtime workspace", p)
    }
    // (4) 关键：返回 resolved-prefix 路径，而不是 lexical abs
    return resolved, nil
}
```

四个动作逐条拆：

**(1) `EvalSymlinks` workspace 根**：`rt.Workspace()` 返回的可能是 `/var/ws`，但 `/var/ws` 本身可能是个软链指向 `/host/home_attacker`。先把根展平，后续 `Rel` 比较才有效。

**(2) 相对路径 join**：把 engine 给的相对路径钉死在 workspace 下；绝对路径走 `(3)` 的祖先解析，最后 `Rel` 比较仍然要求落在 ws 内。

**(3) `resolveExistingPrefix` —— 解析「最深的存在祖先」**（[`custom.go:1156-1173`](../../internal/agent/custom.go)）：

```go
func resolveExistingPrefix(abs string) string {
    suffix := ""
    cur := abs
    for {
        if resolvedAncestor, err := filepath.EvalSymlinks(cur); err == nil {
            if suffix == "" {
                return resolvedAncestor
            }
            return filepath.Join(resolvedAncestor, suffix)
        }
        parent := filepath.Dir(cur)
        if parent == cur {
            return abs
        }
        suffix = filepath.Join(filepath.Base(cur), suffix)
        cur = parent
    }
}
```

**为什么要走这一步？** 因为 output file 在 engine 跑之前**还不存在**（`outputs/newdir/result.json` 整条路径可能一个文件都没有），`EvalSymlinks` 直接对它调用会失败。但**中间目录**（`outputs/newdir`）可能已经被 engine 种成了软链。所以这个函数**从叶子向上走**，找到第一个存在的祖先，对祖先 `EvalSymlinks`，再把不存在的尾巴接回去。

**攻击场景对照**：engine 在 workspace 里建了 `outputs/evil -> /etc`，然后让 output_file 是 `outputs/evil/passwd`。`resolveExistingPrefix` 会找到 `outputs/evil`（它存在），`EvalSymlinks` 把它解析成 `/etc`，最后 `filepath.Rel("/var/ws", "/etc/passwd")` 返回 `../etc/passwd`，触发 `(4)` 的拒绝。

**(4) 返回 resolved 而不是 lexical abs**：这一步是**关 TOCTOU 窗口的关键**。注释（[`custom.go:1140-1148`](../../internal/agent/custom.go)）原话：

> Returning the lexical `abs` instead would re-introduce a TOCTOU window: the custom command could create a not-yet-existing parent as a symlink after this check, and a later `DownloadFile` / `rm` against `abs` would follow it.

换句话说：如果你返回 lexical abs，调用方拿着这个字符串去做 `rm` / `read` 时，engine 可能在这中间又种了链接——你这一次的检查白做了。返回**已经把祖先软链展平**的路径，调用方 follow 时走的是已经被验证过的物理位置。

##### 真正关 TOCTOU 的是「每个 use 前重新解析」

光有 `workspacePath` 还不够——它只是「某次调用时刻的 snapshot」。TOCTOU 的核心是**两次调用之间的时间差**。skill-up 的做法是：**在每一个真正会触碰文件系统的 use site 都重新调用 `workspacePath`**，而不是缓存第一次的结果。三个关键 use site：

1. **`clearStaleOutputFile`**（[`custom.go:258-279`](../../internal/agent/custom.go)）—— 清理上次残留输出文件前 re-resolve（注释明确「一个尚未存在的父目录可能**在那时到现在之间**被并发 case 种成外向软链」）。
2. **`readRawResult`**（[`custom.go:433-446`](../../internal/agent/custom.go)）—— engine 跑完读结果文件前 re-resolve（这次是 **engine 已经跑过之后** 的解析，engine 在运行中种下的链接这里会被看见）。
3. **`archiveRenamedPathArtifact`**（[`custom.go:763-782`](../../internal/agent/custom.go)）—— 归档 path 类 artifact 前 re-check。

**核心设计**：**每一次 `DownloadFile` / `rm` 之前都重新解析**。即使攻击者在两次解析之间种链接，下一次解析也会发现并拒绝。窗口被压缩到「单次解析 + 单次 IO」之间，而 `resolveExistingPrefix` 返回的是已展平路径，follow 时走的是物理路径，连这层窗口都消掉了。

> **Java 类比**：Java 里写文件前用 `Path.normalize()` / `toRealPath()` 防穿越是常见做法，但大多数教程只检查一次。skill-up 这套「每个 use site 都 re-resolve」相当于在每一次 `Files.copy` 前都重新 `toRealPath()`——这才是堵 TOCTOU 的正确姿势。
>
> **面试核心认知**：「TOCTOU 不是靠更严格的检查堵的，是靠**把 check 挪到离 use 越近越好**堵的，理想是 check 和 use 之间没有任何 attacker-controllable 的 IO 间隔。」

---

#### 3.6.3 防御 T2：`clearStaleOutputFile` 防 stale fixture 误读

##### 攻击场景

上次 case 跑完留下了一个 `outputs/result.json`（exit_code=0，答案也对）。这次 case 跑，engine 因为配置错误根本没启动 / 启动就崩了 / 输出写到别的地方，`outputs/result.json` **原地不动**。如果 skill-up 直接读这个文件，就会把**上次的结果当成这次的成功**，评测完全失真——失败 case 骗过 pass rate。

##### 防御（[`custom.go:254-279`](../../internal/agent/custom.go)）

```go
func (a *CustomAgent) clearStaleOutputFile(ctx context.Context, rt Runtime, outputFile string) bool {
    // Re-resolve symlinks now: ...
    safe, err := workspacePath(rt, outputFile)
    if err != nil {
        logging.WarnContextf(ctx, "CustomAgent: refusing to clear output file %s before run: %v", outputFile, err)
        return false
    }
    outputFile = safe
    q := shellQuote(outputFile)
    cmd := "if [ -e " + q + " ]; then rm -f -- " + q + " && printf %s " + shellQuote(customStaleClearedMarker) + "; fi"
    result, err := rt.Exec(ctx, cmd, ExecOptions{})
    // ...
    return strings.Contains(result.Stdout, customStaleClearedMarker)
}
```

两个**前置条件**（在调用点）很关键：

1. **只有用户显式配置了 output_file 才清理**：默认的 `${output_file}` 路径（`outputs/session-result.json`）不动——它可能是用户作为 fixture 输入放进来的合法文件。
2. **当 output 和 input 是同一个文件时不清理**：否则会把刚刚写好的 SessionInput 自己删掉。

`marker` 技巧（`customStaleClearedMarker`）让函数返回 bool 表示「是否真的删过」，后面的 workspace-diff 收集器靠它区分「框架写的文件」vs「用户的产出」。

> **Java 类比**：JUnit 跑测前 `@BeforeEach` 里清空 `target/test-out/`，避免上次跑的产物干扰这次断言。

---

#### 3.6.4 防御 T3：`maskAPIKey` 全字段脱敏

##### 攻击场景

engine 进程的 env 里有 `$ANTHROPIC_API_KEY=sk-ant-xxx`（这是 skill-up 注入的）。engine 的工作里可能**完全无意**地把它 echo 回来——比如 wrapper 脚本 debug 模式打印环境变量；或者 LLM 输出里恰好把 key 复述了一遍。这个字符串一旦进入 `final_message` / `stderr` / `transcript`，就会：

1. 写进 `result.json`（落盘）；
2. 发给 Judge LLM（**第三个组织的 API**，可能记录输入）；
3. 进 OTel span 属性（可观测性后端）；
4. 进 CI 日志（GitHub Actions 日志公开可见）。

##### 防御代码（[`custom.go:942-962`](../../internal/agent/custom.go)）

```go
func (a *CustomAgent) maskAPIKey(s string) string {
    if s == "" {
        return s
    }
    const redacted = "***REDACTED***"
    const minMaskLen = 8
    // (1) 配置的模型层 API key
    if a.Cfg.APIKey != "" && strings.Contains(s, a.Cfg.APIKey) {
        s = strings.ReplaceAll(s, a.Cfg.APIKey, redacted)
    }
    // (2) 所有 engine.custom.env 的 value
    if a.Cfg.Custom != nil {
        for _, v := range a.Cfg.Custom.Env {
            if len(v) < minMaskLen {
                continue
            }
            if strings.Contains(s, v) {
                s = strings.ReplaceAll(s, v, redacted)
            }
        }
    }
    return s
}
```

两个脱敏源：

1. **`a.Cfg.APIKey`**：模型层凭据。**即使 custom engine 从来没拿到它**（model 层和 engine.custom 是分开的），也照脱——注释说「防止一个 inspect 父进程 env 的 wrapper 把它 echo 出来」。这是**纵深防御**（defense in depth）。
2. **`a.Cfg.Custom.Env` 的每个 value**：用户在 `engine.custom.env` 里声明的所有 env 变量值。这是用户**显式**的 secret channel（`MY_TOKEN: ${MY_TOKEN}`）。

**`minMaskLen = 8` 的精妙**：注释解释「短于 8 字符的 value 跳过，避免一个非敏感的 flag 或 model 名把输出里所有短词都变成 `***REDACTED***`」。比如 env 里有个 `VERBOSE=1`，不跳过的话，输出里每个 `1` 都会被替换成 `***REDACTED***`——既要防泄漏又要保证可用性。

##### 全字段覆盖

`maskAPIKey` 不是只用在某个字段，而是**每一个流向外部的不可信字符串**都过一遍。`assembleResult` 和 `parseSessionResult` 里密集出现：

```go
// custom.go:400-409 (text 格式)
finalMsg := a.maskAPIKey(strings.TrimSpace(stdout))
return &SessionResult{
    // ...
    FinalMessage: finalMsg,
    Stderr:       a.maskAPIKey(stderr),
    // ...
}

// custom.go:520-557 (session_result 格式)
finalMsg := a.maskAPIKey(parsed.FinalMessage)
trans := a.maskTranscript(parsed.Transcript)
res := &SessionResult{
    Engine:       firstNonEmpty(a.maskAPIKey(parsed.Engine), a.Name()),
    Model:        firstNonEmpty(a.maskAPIKey(parsed.Model), formatAgentModel(...)),
    // ...
}
```

注意 **`Engine` 和 `Model` 也脱敏**——注释（`custom.go`）明确：engine 返回 `{"engine": "my-wrapper sk-ant-real-key", ...}`，如果不脱 Engine 字段，这个 key 就会写进 `result.json` 和 OTel span 的 `skill_up.engine` 属性。脱敏后变成 `my-wrapper ***REDACTED***`。

HTTP transport 的 error 字符串也脱敏（[`custom_http.go:134-144`](../../internal/agent/custom_http.go)），因为 `net/http` 的 error 会内嵌 request URL：

```go
return &transportOutcome{
    exitCode: 1,
    stderr:   a.maskAPIKey(err.Error()),
    execErr:  errors.New(a.maskAPIKey("custom engine http request failed: " + err.Error())),
}
```

> **Java 类比**：这就像 Spring 里写一个 `@ControllerAdvice` 全局异常处理器，但**主动重写** error message，把任何匹配 credential 模式的子串替换成 `***REDACTED***` 再返回给客户端。差别是 skill-up 是**字段级别、按数据流贴**的，不靠全局拦截——这样即使将来加了新字段，老字段的脱敏也不会失效。

---

#### 3.6.5 防御 T4：凭据不进命令行 / URL

##### 攻击场景

本地 transport 要起进程。如果用户配置：

```yaml
engine:
  custom:
    transport: local
    local:
      command: "curl https://api.x.com/v1/chat -H 'Authorization: Bearer ${api_key}'"
```

`${api_key}` 被渲染成真实 key 后，整个字符串变成 `curl ... -H 'Authorization: Bearer sk-real-key'`。这个字符串会：

1. 进入 `rt.Exec(ctx, cmd, ...)` 的命令行；
2. 出现在**进程列表** `ps aux`（同主机任何用户都能看）；
3. 进入 exec span 的 `process.command` 属性（OTel 后端）；
4. 进入失败时的 error 日志。

##### 防御：`containsAPIKey` + `errSecretInCommand`（[`custom.go:908-910, 965-970, 288-300`](../../internal/agent/custom.go)）

```go
func (a *CustomAgent) containsAPIKey(s string) bool {
    return a.Cfg.APIKey != "" && strings.Contains(s, a.Cfg.APIKey)
}

func errSecretInCommand(field string) error {
    return fmt.Errorf(
        "engine.custom.%s renders the API key into the command line, which would expose it in process listings and traces; reference ${api_key} from engine.custom.env instead",
        field,
    )
}

// buildLocalExec 里
command, err := renderTemplate(local.Command, vars)
// ...
if a.containsAPIKey(command) {
    return "", ExecOptions{}, errSecretInCommand("local.command")
}
parts := []string{shellQuote(command)}
for i, raw := range local.Args {
    arg, rErr := renderTemplate(raw, vars)
    // ...
    if a.containsAPIKey(arg) {
        return "", ExecOptions{}, errSecretInCommand(fmt.Sprintf("local.args[%d]", i))
    }
    parts = append(parts, shellQuote(arg))
}
```

**关键设计**：检查的是**渲染后**的字符串，不是模板原文。这样无论是 `${api_key}` 直接展开，还是间接拼出来（比如 kwargs 里塞了 key 再 `${kwargs.token}` 展开），只要最终 command/arg 里出现了 API key 的完整值，就拒绝。

##### URL 也查（[`custom_http.go:51-53, 254-258`](../../internal/agent/custom_http.go)）

```go
if a.containsAPIKey(url) {
    return nil, errSecretInURL()
}

func errSecretInURL() error {
    return errors.New(
        "engine.custom.http.url renders the API key into the URL, which would expose it in request logs and traces; reference ${api_key} from engine.custom.http.headers or request_body instead",
    )
}
```

**正确的 channel** 在 error message 里直接告诉用户：用 `engine.custom.env`（本地）或 `http.headers` / `http.request_body`（HTTP）传 key，这些字段不会进命令行/URL。

##### artifact URL 也查（[`custom.go:846-849`](../../internal/agent/custom.go)）

```go
if a.containsAPIKey(f.URL) {
    logging.WarnContextf(ctx, "CustomAgent: artifact %q url embeds the API key; skipping download", f.Name)
    return ""
}
```

否则这个 key 会被传到 engine 声明的 endpoint，以及它的 request log 和代理里。

> **Java 类比**：这相当于在 `Runtime.exec` 之前做一道 gate，扫描命令行参数里有没有出现 `System.getenv("API_KEY")` 的值。Java 圈类似的实践是「永远不要把 secret 拼进命令行，要用 stdin / env var 传」——但很少有项目显式做这种「渲染后扫描」的检查。
>
> **面试要点**：「`ps` 是同主机最古老的 secret leakage 渠道，Go 的 `os/exec` 把 args 当 argv 传本来已经避开了 shell 注入，但 args 仍然会出现在 `/proc/<pid>/cmdline`，所以渲染后的字符串本身不能含 key。」

---

#### 3.6.6 防御 T5：artifact URL 不跟随 3xx 重定向（SSRF）

##### 攻击场景

engine 返回 `artifacts.files[].url: "http://attacker.com/redirect"`，attacker.com 返回 `302 Location: http://169.254.169.254/latest/meta-data/iam/security-credentials/`（AWS 元数据服务，云主机内网地址）。如果下载器跟随重定向，它会把云主机的临时凭据下载下来，作为 artifact 写进报告目录——攻击者读一下评测报告就拿到了 IAM 凭据。

即使不针对云元数据，重定向也能让下载器**绕过 skill-up 在原 URL 上做的所有检查**（scheme 白名单、api-key 检查）。

##### 防御（[`custom.go:829-904`](../../internal/agent/custom.go)）

**trust boundary 设定**：engine（和操作者）就是信任边界——engine 声明什么 URL 就 fetch 什么 URL，这是「exact declared URL」契约。但**重定向打破这个契约**：一旦跟随 3xx，实际 fetch 的就不再是 engine 声明的那个 URL，而是某个第三方（attacker 或被劫持的 host）指定的任意地址。核心防御是**关闭重定向跟随**（[`custom.go:867-872`](../../internal/agent/custom.go)）：

```go
// Redirects are NOT followed: a 3xx would let the artifact host (or the engine)
// swap the archived bytes for a Location pointing at a different — possibly
// internal — endpoint, breaking the "exact declared URL" boundary and the
// up-front api-key/scheme checks. ErrUseLastResponse returns the 3xx as-is so
// it falls through to the non-2xx skip path below.
client := &http.Client{
    Timeout: customArtifactDownloadTimeout,
    CheckRedirect: func(*http.Request, []*http.Request) error {
        return http.ErrUseLastResponse
    },
}
```

`http.ErrUseLastResponse` 是 Go stdlib 的一个哨兵 error，告诉 `http.Client`：「不要跟随，把最近的响应原样返回给我」。这样 3xx 就会落到下面 `if resp.StatusCode < 200 || resp.StatusCode >= 300` 的分支，被当作非 2xx 跳过。

**前置检查（纵深防御）**（[`custom.go:833-849`](../../internal/agent/custom.go)）：

```go
parsed, err := url.Parse(f.URL)
// ...
if parsed.Scheme != "http" && parsed.Scheme != "https" {
    logging.WarnContextf(ctx, "CustomAgent: artifact %q url scheme %q is not http(s); skipping", f.Name, parsed.Scheme)
    return ""
}
if a.containsAPIKey(f.URL) {
    logging.WarnContextf(ctx, "CustomAgent: artifact %q url embeds the API key; skipping download", f.Name)
    return ""
}
```

- **scheme 白名单**：只允许 `http` / `https`。`file://` / `gopher://` / `dict://` 这些会被 url 驱动访问本地文件或奇怪协议的 scheme 一律拒绝。
- **api-key 检查**：见 §3.6.5。

**为什么要多层检查 + 关重定向？** 因为任何一层都不是完整的——scheme 白名单挡不住 `http://169.254.169.254`（它就是合法的 http），不跟随重定向也挡不住 engine **直接**返回内网 URL（它就是声明的那个 URL）。组合起来才能覆盖：「engine 声明的 URL 必须是 http(s)、不能含 key；只 fetch 这个 URL，不跟随任何跳转」。

> **Java 类比**：Java 圈防 SSRF 的标准做法是「自定义 `HttpClient.setRedirectPolicy(NEVER)` + URL 白名单解析 + 禁用非 http(s) scheme」。Apache HttpClient 的 `HttpClientBuilder.disableRedirectHandling()` 等价于这里的 `ErrUseLastResponse`。
>
> **为什么不做 IP 黑名单？** 因为黑名单永远漏（云厂商不断新增元数据 IP、内网段、IPv6 ULA），且 engine 声明的 URL 本来就是操作者负责的（trust boundary）。skill-up 用「不跟随重定向 + scheme 白名单 + size cap」把可控范围卡死，**不试图枚举所有内网段**——这是更工程务实的姿态。

---

#### 3.6.7 防御 T6：size cap（内存耗尽）

##### 攻击场景

HTTP transport 的 `http.files` 支持用 glob 上传 workspace 文件（比如 `**/*`）。如果 workspace 很大，整个 multipart body 在内存里组装（`bytes.Buffer`），直接 OOM。同样，一个恶意或配置错误的 engine HTTP 服务可以返回一个无上限的响应体。

##### 防御 1：下载 artifact 上限（[`custom.go:886-896`](../../internal/agent/custom.go)）

```go
body, err := io.ReadAll(io.LimitReader(resp.Body, maxArtifactDownloadBytes+1))
if err != nil {
    logging.WarnContextf(ctx, "CustomAgent: cannot read artifact %q body: %v", f.Name, a.maskAPIKey(err.Error()))
    return ""
}
if int64(len(body)) > maxArtifactDownloadBytes {
    logging.WarnContextf(ctx, "CustomAgent: artifact %q exceeds download size limit %d bytes; dropping", f.Name, maxArtifactDownloadBytes)
    return ""
}
```

`maxArtifactDownloadBytes = 256 * 1024 * 1024`（256MB）。**多读 1 字节**是为了能区分「正好达到上限」和「超过上限」——只读到上限会**静默截断**，下载者得到一个被截断的文件还以为下载完了；多读 1 字节后只要 `len(body) > cap` 就 100% 是超限，直接丢弃。error 也过 `maskAPIKey`（URL 嵌在 error 里）。

##### 防御 2：HTTP 响应体上限（[`custom_http.go:47-50, 149-160`](../../internal/agent/custom_http.go)）

```go
const maxHTTPResponseBytes = 64 * 1024 * 1024  // 64MB

respBody, readErr := io.ReadAll(io.LimitReader(resp.Body, maxHTTPResponseBytes+1))
if int64(len(respBody)) > maxHTTPResponseBytes {
    msg := fmt.Sprintf("custom engine http response exceeded %d bytes", maxHTTPResponseBytes)
    return &transportOutcome{exitCode: 1, stderr: msg, execErr: errors.New(msg)}
}
```

一个 SessionResult JSON 64MB 已经远超合理大小（典型几 KB 到几 MB），超过就是配置错误或恶意。同样**多读 1 字节**避免静默截断。

##### 防御 3：inline artifact 上限

三层 cap，逐层收紧（[`custom.go:39-46`](../../internal/agent/custom.go)）：

```go
maxInlineArtifactBytes  = 50 * 1024 * 1024   // 单个 inline artifact
maxInlineArtifactTotal  = 200 * 1024 * 1024  // 单个 SessionResult 内所有 inline 总和
```

- `validateArtifactSize`：解析时用 base64 长度**估算**解码后大小，超限直接 reject（不付解码代价）；
- `writeInlineArtifact`：解码后**真实大小**再校验一次——因为 `len(content_base64) * 3/4` 是估算，padding 和实际解码可能差一点，真实大小再校验保证 100% 拦得住。

> **Java 类比**：这就像在 Spring 里给 `WebClient` 配 `maxInMemorySize(64MB)`（默认 256KB，太小）——任何反序列化前的缓冲都要有上限，否则一个恶意 / 失控的 peer 一个 HTTP 响应就让你 OOM。**多读 1 字节区分「达到上限 vs 超过上限」是处理 bounded read 的通用技巧**。

---

#### 3.6.8 防御 T7/T8：配置侧 `resolveEnvRefsStrict` 防走私

这一节关心的是**另一类威胁**：不是 engine 返回值攻击 skill-up，而是**用户写错配置**把自己 leak 了。

##### 敏感变量名识别（[`config/customengine.go:18-24`](../../internal/config/customengine.go)）

```go
var sensitiveEnvNamePattern = regexp.MustCompile(
    `(?i)(^|_)(API_?KEY|ACCESS_?KEY|KEY|TOKEN|SECRET|PASSWORD|PASSWD|CREDENTIALS?|AUTHORIZATION)(_|$)`,
)

func isSensitiveEnvName(name string) bool {
    return sensitiveEnvNamePattern.MatchString(name)
}
```

匹配 `API_KEY` / `TOKEN` / `SECRET` / `PASSWORD` / `CREDENTIALS` / `AUTHORIZATION` 等敏感词，用 `(^|_)...(_|$)` 词边界锚定。注释特别提到「Word boundaries avoid false positives like MONKEY_PATH」——不用 `(?i).*key.*` 这种粗暴正则，否则 `MONKEY_PATH`、`KEYBOARD_LAYOUT` 全部误判。

##### 凭证形状识别（[`config/customengine.go:30-41`](../../internal/config/customengine.go)）

光靠变量名不够——用户可能把 key 写进一个**看起来无害的变量名**的默认值：`${INNOCUOUS_NAME:-sk-ant-real-key}`。所以还要识别**真实凭证的形状**：

```go
var secretLiteralPatterns = []*regexp.Regexp{
    regexp.MustCompile(`(?:^|[^A-Za-z0-9_])sk-[A-Za-z0-9_\-]{16,}`),
    regexp.MustCompile(`(?:^|[^A-Za-z0-9_])gh[pousr]_[A-Za-z0-9]{20,}`),
    regexp.MustCompile(`(?:^|[^A-Za-z0-9_])AIza[A-Za-z0-9_\-]{20,}`),
    regexp.MustCompile(`(?:^|[^A-Za-z0-9_])xox[abposr]-[A-Za-z0-9\-]{10,}`),
    regexp.MustCompile(`(?:^|[^A-Za-z0-9_])(?:AKIA|ASIA)[A-Z0-9]{16}`),
    // JWT: three base64url segments separated by dots.
    regexp.MustCompile(`(?:^|[^A-Za-z0-9_])ey[A-Za-z0-9_\-]{10,}\.[A-Za-z0-9_\-]{10,}\.[A-Za-z0-9_\-]{10,}`),
}
```

覆盖主流厂商：OpenAI/Anthropic（`sk-...`）、GitHub（`ghp_/gho_/...`）、Google（`AIza...`）、Slack（`xox...`）、AWS（`AKIA/ASIA...`）、JWT（三段 base64url 用 `.` 分隔，以 `ey` 开头因为 `{"..."` base64url 编码后是 `ey`）。

##### 严格模式 `resolveEnvRefsStrict` 迭代展开（[`config/customengine.go:397-410`](../../internal/config/customengine.go)）

```go
func resolveEnvRefsStrict(s string) (string, error) {
    const maxStrictExpansionDepth = 10
    for range maxStrictExpansionDepth {
        resolved, err := resolveEnvRefsWith(s, true)
        if err != nil {
            return "", err
        }
        if resolved == s || !strings.Contains(resolved, "${") {
            return resolved, nil
        }
        s = resolved
    }
    return "", errors.New("strict env resolution exceeded maximum depth (possible reference cycle)")
}
```

**「严格」体现在两件事**：

1. **拒绝敏感名**：如果 token 名匹配 `isSensitiveEnvName`（如 `${MY_API_KEY}`），直接报错。
2. **迭代展开防走私（T8）**：

**攻击场景（T8）**：用户在 shell env 里设了 `WRAPPER=${ANTHROPIC_API_KEY}`（`WRAPPER` 这个名字不敏感），然后在 skill-up 配置里写 `command: ${WRAPPER}`。如果只做**单次**检查，`WRAPPER` 通过（名字不敏感），渲染后变成 `${ANTHROPIC_API_KEY}` 字面量，再被运行时展开——key 进了命令行。**迭代展开**后第二轮看到 `${ANTHROPIC_API_KEY}`，被 `isSensitiveEnvName` 拦下。最多 10 轮，到顶报「possible reference cycle」（防 `A=${B}, B=${A}` 死循环）。

##### 严格模式用在哪：所有「会变成命令行」的字段

`resolveLocalEnv`（[`config/customengine.go:274-289`](../../internal/config/customengine.go)）对 **command / args / cwd / input_file / output_file** 全用 strict：

```go
func resolveLocalEnv(l *customengine.LocalConfig) []string {
    var errs []string
    errs = append(errs, resolveScalarEnvStrict("local.command", &l.Command)...)
    errs = append(errs, resolveScalarEnvStrict("local.cwd", &l.Cwd)...)
    errs = append(errs, resolveScalarEnvStrict("local.input_file", &l.InputFile)...)
    errs = append(errs, resolveScalarEnvStrict("local.output_file", &l.OutputFile)...)
    for i := range l.Args {
        rv, err := resolveEnvRefsStrict(l.Args[i])
        // ...
    }
    return errs
}
```

而 `env` / `http.headers` 这些**合法持有凭据**的字段用普通模式。**kwargs 用 strict**，因为 kwargs 会通过 `${kwargs.token}` 展开进命令行。

##### 命名归一化 `normalizeKeyForSensitiveCheck`（[`config/customengine.go:104-126`](../../internal/config/customengine.go)）

把 kwarg key 统一成 UPPER_SNAKE_CASE：`api-key`（hyphen）→ `API_KEY`、`apiKey`（camelCase）→ `API_KEY`、`bearerToken` → `BEARER_TOKEN`。

**攻击场景**：用户写 `kwargs: {apiKey: "${ANTHROPIC_API_KEY}"}`，然后 `command: "wrapper --token ${kwargs.apiKey}"`。如果归一化只认字面 `API_KEY`，`apiKey` 这个 camelCase key 就**绕过 kwargs 敏感检查**，把 key 渲染进命令行。归一化后 `apiKey` 被识别为敏感，拦下。

> **Java 类比**：Spring 的 `RelaxedPropertyResolver` / `Binder` 能把 `api-key` / `apiKey` / `API_KEY` 都绑定到同一个 `@ConfigurationProperties`。skill-up 用归一化做**安全检查**而不是绑定，但思路一致：「不能因为用户命名风格不同就让检查失效」。

---

#### 3.6.9 凭据管理 credential.go：多源 + 脱敏日志 + 从不打明文

##### 多源优先级

凭据来源按优先级（高 → 低）：

| 源 | 入口 | 备注 |
|----|------|------|
| **CLI flag** `--api-key` | `applyCLIOverrides`（[`agent_init.go:181-200`](../../internal/credential/agent_init.go)） | 最高，覆盖一切 |
| **Provider-scoped env** `${PROVIDER}_API_KEY` | `lookupProviderEnv`（[`agent_init.go:245-255`](../../internal/credential/agent_init.go)） | 比如 `ANTHROPIC_API_KEY`、`OPENAI_API_KEY` |
| **Resolver**（`~/.skill-up/credentials.yaml`） | `resolveValue`（[`agent_init.go:220-243`](../../internal/credential/agent_init.go)） | 用户私有配置文件 |
| **Runner fallback** | `applyFallbackCredentials`（[`agent_init.go:171-179`](../../internal/credential/agent_init.go)） | Judge 复用 Runner 的 key |

`.env` 通过 `godotenv.Load()` 加载（[`credential.go:61`](../../internal/credential/credential.go)），`credentials.yaml` 通过 `loadFromFile` 解析（[`credential.go:138-178`](../../internal/credential/credential.go)）。两个源**互补**：env 适合 CI（环境变量注入），yaml 适合本地开发（一个文件管多 provider）。

##### `MaskAPIKey`：首 2 后 2（[`credential.go:91-98`](../../internal/credential/credential.go)）

```go
const apiKeyMaskLen = 4 // Length for API key masking

func MaskAPIKey(key string) string {
    if len(key) <= apiKeyMaskLen {
        return "****"
    }
    return key[:2] + "****" + key[len(key)-2:]
}
```

**短于等于 4 字符**全打码（避免 `key[:2]` 越界 + 短 key 暴露比例太大）；长 key 显示首 2 后 2。比如 `sk-ant-api03-xxxxxxxxxxxx` → `sk****xx`。

**为什么是首尾各 2 而不是「中间打码保留前缀」？** 因为厂商前缀本身就识别身份（`sk-ant-` = Anthropic），保留前 2 字符刚好够让人**认出这是哪种 key**（debug 用），但不够复原。完全打码（`****`）反而难 debug。

> **注意与 `CustomAgent.maskAPIKey`（§3.6.4）的区别**：`MaskAPIKey` 是**配置层日志脱敏**（保留首尾可识别），`maskAPIKey` 是**结果层全替换**（`***REDACTED***`，更激进）。两者用在不同位置是有意的——日志保留可识别度便于排错，结果文件 / Judge 输入要彻底不能复原。

##### 脱敏日志（[`credential.go:101-115`](../../internal/credential/credential.go) + [`agent_init.go:289-306`](../../internal/credential/agent_init.go)）

```go
logging.Debugf("CREDENTIAL_DISCOVERED provider=%s source=resolver api_key=%s", name, MaskAPIKey(cred.APIKey))
// ...
logging.Debugf("AGENT_CONFIG kind=%s engine=%s api_key=%s source.api_key=%s",
    params.Kind, params.Engine, MaskAPIKey(params.APIKey), params.APIKeySource)
```

**每一个打印 api_key 的地方都过 `MaskAPIKey`**——没有 `Debugf("api_key=%s", cred.APIKey)` 这种裸打。注释（[`agent_init.go:281-282`](../../internal/credential/agent_init.go)）连字段名都换成 `auth_env`：

```go
logging.Debugf("AGENT_CONFIG kind=%s engine=%s provider=%s auth_env=%s source.auth=%s",
    params.Kind, params.Engine, params.Provider, envVar, ValueSourceEnv)
```

注意 `auth_env` 而不是 `api_key_env`——**连环境变量名都不暴露**，因为某些环境变量名（如 `ANTHROPIC_API_KEY`）本身能泄露 provider 信息。

> **Java 类比**：整套设计就是 Spring Security 里 `PropertySource` 优先级链（CLI > env > yaml > default）+ 日志脱敏的组合。差别是 skill-up 是无框架、纯 Go 实现。
>
> **面试核心认知**：「凭据管理的核心不是加密存储（那是 vault / KMS 干的），而是**确保明文在内存里流转时永远不出现在日志 / 进程列表 / 报告文件里**——这要靠『每一个 print 点都过 mask』的纪律，不是靠一处全局拦截。」

---

#### 3.6.10 shellquote：统一引用，永不字符串拼接不可信命令

[`internal/shellquote/shellquote.go`](../../internal/shellquote/shellquote.go)

##### POSIX 引用（[`shellquote.go:7-9`](../../internal/shellquote/shellquote.go)）

```go
func QuotePOSIX(s string) string {
    return "'" + strings.ReplaceAll(s, "'", `'\''`) + "'"
}
```

把任意字符串包成 POSIX shell 单引号字符串。唯一需要 escape 的是单引号本身——用 `'\''`（关闭引号 → 转义单引号 → 重开引号）这个标准技巧。

**为什么单引号而不是双引号？** 单引号里**没有任何转义**（除了单引号本身），shell 不会解释 `$var`、反引号、`\`，最安全。双引号会展开 `$`、反引号，留攻击面。

##### Windows 引用（[`shellquote.go:24-49`](../../internal/shellquote/shellquote.go)）

按 `CommandLineToArgvW` 的解析规则引用。注释里的真实 bug 故事：

> The result is always wrapped, even when s has no whitespace or metacharacter triggers: when NoneRuntime.Exec routes through bash on Windows (the default when Git Bash is discoverable), an unquoted backslash-bearing path such as `C:\tmp\script.cmd` would have its backslashes stripped by bash and reach the downstream `cmd /c` / `powershell -File` as `C:tmpscript.cmd`.

**永远包裹**（即使没有空格）——因为 bash 在 Windows 上会吃掉裸路径里的 `\`。这是从真实 bug 里学出来的规则。

##### 在 CustomAgent 里的使用（项目级硬规则）

`buildLocalExec` 里**每个 arg 都过 shellQuote**（[`custom.go:291, 300`](../../internal/agent/custom.go)）：

```go
parts := []string{shellQuote(command)}
for i, raw := range local.Args {
    arg, rErr := renderTemplate(raw, vars)
    // ...
    parts = append(parts, shellQuote(arg))
}
```

`clearStaleOutputFile` 里拼 `rm` 命令也用（[`custom.go:271-272`](../../internal/agent/custom.go)）：

```go
q := shellQuote(outputFile)
cmd := "if [ -e " + q + " ]; then rm -f -- " + q + " && printf %s " + shellQuote(customStaleClearedMarker) + "; fi"
```

**核心纪律**（项目原话）：「When adding new shell execution paths, use `internal/shellquote` — never concatenate untrusted strings into a shell command.」任何「把外部字符串拼进 shell 命令」的地方都必须过 `shellQuote`，否则 review 会被打回。

> **Java 类比**：这相当于永远用 `ProcessBuilder(List<String>)` 而不是 `Runtime.exec(String)` + 字符串拼接。Go 的 `os/exec` 默认走 `execve`（argv 数组，不经 shell），但 skill-up 的 NoneRuntime 在 Windows 上要路由经过 bash，所以仍需要 shell 引用。关键是「**不可信字符串永远只作为单个 argv 元素**，绝不拼进 shell 字符串」——这条规则在 Java 圈也是一样。

---

## 4. 动手练习

### 练习 1：跑一次 `benchmark.enabled=true` 的评测，对比 with/without skill 的 benchmark.json

**目标**：体感 A/B 量化是怎么算出来的，眼见为实。

**步骤**：

1. 找一个 demo（比如 `demo/chinese-jokes-workspace`），把它的 `eval.yaml` 复制一份，加上：

   ```yaml
   benchmark:
     enabled: true            # 开启 A/B
     runs_per_configuration: 3 # 每边跑 3 次（凑出 stddev）
   ```

2. 跑 `skill-up run --config your-eval.yaml`，等待结束。

3. 打开 `<skill>-workspace/iteration-1/benchmark.json`，找到 `run_summary` 字段，对照 [`benchmark_anthropic.go`](../../internal/report/benchmark_anthropic.go) 的结构，回答：

   - `with_skill.pass_rate.mean` 和 `without_skill.pass_rate.mean` 分别是多少？
   - `delta.pass_rate` 字符串（带 `+/-` 符号）方向对不对？正号说明 Skill 提升了通过率还是降低了？
   - `stddev` 大不大？如果 stddev 很大（比如 pass_rate 在 [0.2, 1.0] 之间跳），说明什么？

4. 打开 `benchmark.md`（人读版），对比表格的两列。

**通过标准**：你能指着 `delta.pass_rate` 的正负号说「这个 Skill 比基线好/差 X%」，并解释 stddev 大意味着结果不稳。

> **进阶**：故意改一个 Skill 让它变差（比如把 `SKILL.md` 里关键步骤删掉），重跑一次，看 delta 是不是真的变负了。这是「量化反馈循环」的体感。

---

### 练习 2：对着 `workspacePath` 画出 TOCTOU 攻击怎么被挡住

**目标**：把 §3.6.2 的抽象描述变成你能讲出来的具体场景图。

**步骤**：

拿张纸或开白板，画出下面这 5 个时间点和文件系统状态：

```
t0: workspace = /var/ws（干净，无软链）
t1: skill-up 调 workspacePath(rt, "outputs/newdir/result.json")
    - resolveExistingPrefix 从叶子向上找
    - outputs/newdir 还不存在 → 继续向上 → outputs 存在，EvalSymlinks(outputs) = /var/ws/outputs
    - 返回 /var/ws/outputs/newdir/result.json（resolved-prefix）
t2: ★ engine 在运行中种了软链：ln -s /etc /var/ws/outputs/newdir
t3: skill-up 在 readRawResult 里【再次】调 workspacePath
    - 这次 outputs/newdir 存在了，EvalSymlinks(outputs/newdir) = /etc
    - resolved = /etc/result.json
    - filepath.Rel("/var/ws", "/etc/result.json") = "../etc/result.json"
    - 命中 ".." 检查 → 拒绝！
t4: ★ 如果 skill-up 只在 t1 检查一次（错误做法），t3 就会跟随软链读 /etc/result.json
```

**要求**：

1. 在图上标出「t1 检查通过」「t2 攻击发生」「t3 重新检查发现并拒绝」三个关键节点。
2. 在 `resolveExistingPrefix` 那一步画一个箭头，说明「为什么不能直接对叶子 EvalSymlinks」（答：叶子还不存在）。
3. 写出：如果 skill-up 缓存了 t1 的解析结果、t3 直接用，会发生什么（答：t3 会 follow 被种的软链读到 /etc/result.json）。

**通过标准**：你能对着图讲清楚「TOCTOU 不是靠更严格的检查堵的，是靠把 check 挪到离 use 越近越好堵的」这句话。

> **进阶**：再画一张「为什么必须返回 resolved 而不是 lexical abs」的图（提示：如果返回 lexical abs，调用方拿着这个字符串去做 `rm` 时，engine 可能在这中间又种了链接）。

---

## 5. 检查点自测（附答案要点）

先自己答，再对答案要点。答对 4 题以上算本课过关。

**Q1. 为什么要兼容 Anthropic 格式？skill-up 自己定一套 schema 不行吗？**

<details>
<summary>答案要点</summary>

- 兼容 Anthropic skill-creator 的 schema（grading.json / eval_metadata.json / benchmark.json），让本地评测产物可以**直接被 eval-viewer 等官方工具消费**——这是「**生态借力**」策略。
- 自建 schema 的问题：用户只能用 skill-up 自己的工具看，**生态锁死**；Anthropic 工具链（CI 集成、可视化）要重新接；在 Anthropic 生态里无法被对比引用。
- 兼容的成本很低：一个 `ConvertToAnthropicGrading` 适配器（[`grading.go:47-82`](../../internal/report/grading.go)）做字段级 1:1 映射，10 行手写代码 vs 引入 MapStruct 框架。
- 关键认知：「**少发明标准，多对齐事实标准**」是开源项目进入主流生态的关键。Java 类比：Spring 对齐 JPA、Spring Boot Starter 对齐 MicroProfile。

</details>

**Q2. A/B 的 delta 是怎么算的？说明什么？为什么 stddev 不参与 delta 计算？**

<details>
<summary>答案要点</summary>

- delta = `with_skill.mean - without_skill.mean`，对 4 个指标（pass_rate / time_seconds / input_tokens / output_tokens）分别算（[`benchmark.go:112-119`](../../internal/report/benchmark.go)）。
- Anthropic 版用字符串格式化（`%+.2f` / `%+.1f`），强制带正负号，让人一眼看出方向（[`benchmark_anthropic.go:179-183`](../../internal/report/benchmark_anthropic.go)）。
- delta 说明「**这个 Skill 比 baseline 好多少 / 差多少**」：正号 pass_rate = Skill 提升了通过率；负号 time = Skill 加快了响应。
- **stddev 不参与 delta**，因为 delta 是「描述性差异」，不是「统计显著性检验」。生产级 A/B 还要做 t-test 判断差异是否显著（vs 噪声），skill-up 选了「够用即可」——mean+stddev 足够给肉眼判断。
- stddev 的作用是回答「**结果稳吗**」：stddev 大意味着 Skill 效果不稳定（比如 pass_rate 在 [0.2, 1.0] 跳），即使 mean 高也不可信。

</details>

**Q3. TOCTOU 是什么？skill-up 怎么防的？为什么「检查更严格」没用？**

<details>
<summary>答案要点</summary>

- TOCTOU = Time-of-Check to Time-of-Use，是「检查时刻」和「使用时刻」之间攻击者改了状态的竞态漏洞。
- skill-up 防御（[`custom.go:1115-1149`](../../internal/agent/custom.go)）：
  1. **`EvalSymlinks` 展平 workspace 根和中间祖先**（`resolveExistingPrefix`），把已存在的符号链接物理位置定下来；
  2. **返回已展平的路径**（不是 lexical abs），调用方 follow 时走物理路径；
  3. **核心**：每个真正会 `rm` / `read` 的 use site（`clearStaleOutputFile`、`readRawResult`、`archiveRenamedPathArtifact`）**都重新调用 `workspacePath`**——即使 engine 在两次调用之间种了链接，下一次调用会看见并拒绝。
- **「检查更严格」没用**，因为问题不在「单次检查的严格度」，在「两次检查之间的时间窗口」。攻击者可以在你检查完之后、使用之前改状态——单次检查再严格，也挡不住「检查后」发生的事。
- **核心认知**：「TOCTOU 不是靠更严格的检查堵的，是靠**把 check 挪到离 use 越近越好**堵的。理想是 check 和 use 之间没有任何 attacker-controllable IO 间隔。」
- Java 类比：`Files.copy` 前重新 `toRealPath()`，而不是「函数入口处 `toRealPath()` 一次，后面 5 个 IO 都用缓存值」。

</details>

**Q4. 凭据为什么不进命令行？skill-up 在哪几道闸门拦这件事？**

<details>
<summary>答案要点</summary>

- 因为命令行会出现在 `ps aux`（同主机任何用户都能看）、`/proc/<pid>/cmdline`、exec span 的 `process.command` 属性、失败时的 error 日志。这是**同主机最古老的 secret leakage 渠道**。
- skill-up 在 4 道闸门拦：
  1. **local command/args**：`buildLocalExec` 渲染完用 `containsAPIKey` 扫描（[`custom.go:288, 297`](../../internal/agent/custom.go)），命中就 `errSecretInCommand` 拒绝运行。
  2. **http URL**：`httpTransport.run` 用 `errSecretInURL`（[`custom_http.go:51-53`](../../internal/agent/custom_http.go)）挡 URL 里的 key。
  3. **artifact URL**：下载前也过 `containsAPIKey`（[`custom.go:846-849`](../../internal/agent/custom.go)）。
  4. **配置侧 strict 模式**：`resolveEnvRefsStrict`（[`config/customengine.go:397-410`](../../internal/config/customengine.go)）拦截 `${API_KEY}` 直接引用 + 迭代展开防 wrapper 走私。
- **正确的 channel** 在 error message 里告诉用户：用 `engine.custom.env`（本地）或 `http.headers` / `http.request_body`（HTTP）传 key。
- Java 类比：永远用 `ProcessBuilder(List<String>)` 而不是 `Runtime.exec(String)` + 字符串拼接；secret 永远走 env var / stdin，不进 args。

</details>

**Q5. SSRF 怎么防？为什么不做内网 IP 黑名单？**

<details>
<summary>答案要点</summary>

- artifact URL 下载（[`custom.go:829-904`](../../internal/agent/custom.go)）用**四层组合**，不靠 IP 黑名单：
  1. **scheme 白名单**：只允许 `http` / `https`（挡 `file://` / `gopher://`）；
  2. **不跟随重定向**（核心）：`http.Client.CheckRedirect` 返回 `http.ErrUseLastResponse`，3xx 原样返回走非 2xx 分支跳过——否则 attacker.com 302 跳到 `169.254.169.254` 元数据服务，会下云凭据；
  3. **api-key 检查**：URL 含 key 不下载；
  4. **size cap**：256MB 上限多读 1 字节判断，防内存耗尽。
- **不做内网 IP 黑名单的原因**：
  - 黑名单永远漏（云厂商不断新增元数据 IP、内网段、IPv6 ULA）；
  - engine 声明的 URL 本来就是操作者负责的（**trust boundary**）——skill-up 声明「engine/operator 是信任边界，exact declared URL 是契约」，在这个假设下卡死「重定向 / scheme / size」三个开关即可；
  - 枚举内网段是「永远补不完的黑名单」，组合策略把可控范围卡死在「声明的 URL + 不跳转 + 不超大」，**更工程务实**。
- Java 类比：Apache HttpClient 的 `HttpClientBuilder.disableRedirectHandling()` 等价于 `ErrUseLastResponse`；Spring Security 的 filter chain 也是「多层独立可绕过」的组合防御思路。

</details>

---

## 6. 深入阅读

本课是「报告 + 安全」的压缩包，下面两份是「源码逐行原版详解」，想加深时点开看：

| 想加深的方向 | 去看 |
|---|---|
| Reporter 策略 / JUnit 三态 / HTML 自包含 / Anthropic 兼容 / Benchmark 计算 / IterationWorkspace 完整源码深挖 | [dev-doc/deep/07-Report生成与Anthropic兼容.md](../deep/07-Report生成与Anthropic兼容.md) |
| Custom Engine 不可信输入全链路防御（TOCTOU / 凭据脱敏 / SSRF / OOM / 配置侧防走私 / credential / shellquote）逐行讲解 | [dev-doc/deep/08-安全-CustomEngine不可信输入.md](../deep/08-安全-CustomEngine不可信输入.md) |

**源码索引**（按本课讲解顺序）：

| 关注点 | 文件:行号 |
|---|---|
| Reporter 接口 | [`internal/report/reporter.go:12-14`](../../internal/report/reporter.go) |
| Input 数据模型 | [`internal/report/reporter.go:17-54`](../../internal/report/reporter.go) |
| JSONReporter | [`internal/report/json.go:12-32`](../../internal/report/json.go) |
| JUnitReporter + 三态映射 | [`internal/report/junit.go:18-210`](../../internal/report/junit.go) |
| HTMLReporter + //go:embed | [`internal/report/html.go:19-46, 304-314`](../../internal/report/html.go) |
| with/without 配对 | [`internal/report/html.go:185-247`](../../internal/report/html.go) |
| AnthropicGrading 适配器 | [`internal/report/grading.go:26-82`](../../internal/report/grading.go) |
| Benchmark 4 指标 + mean/stddev | [`internal/report/benchmark.go:32-162`](../../internal/report/benchmark.go) |
| Anthropic Benchmark + Functional Options | [`internal/report/benchmark_anthropic.go:22-220`](../../internal/report/benchmark_anthropic.go) |
| IterationWorkspace 目录治理 | [`internal/report/workspace.go:54-234`](../../internal/report/workspace.go) |
| validateCaseID / isRootedPath 防穿越 | [`internal/report/workspace.go:33-51`](../../internal/report/workspace.go) |
| writeToOutput 管道友好 | [`internal/report/helpers.go:28-44`](../../internal/report/helpers.go) |
| Runner.WriteResults 编排 | [`internal/runner/runner.go:191-238`](../../internal/runner/runner.go) |
| workspacePath TOCTOU 防御 | [`internal/agent/custom.go:1115-1149`](../../internal/agent/custom.go) |
| resolveExistingPrefix | [`internal/agent/custom.go:1156-1173`](../../internal/agent/custom.go) |
| clearStaleOutputFile | [`internal/agent/custom.go:254-279`](../../internal/agent/custom.go) |
| maskAPIKey 全字段脱敏 | [`internal/agent/custom.go:942-962`](../../internal/agent/custom.go) |
| containsAPIKey / errSecretInCommand | [`internal/agent/custom.go:908-910, 965-970`](../../internal/agent/custom.go) |
| artifact URL 不跟随 3xx | [`internal/agent/custom.go:829-904`](../../internal/agent/custom.go) |
| size cap 下载上限 | [`internal/agent/custom.go:886-896`](../../internal/agent/custom.go) |
| 配置侧 sensitiveEnvNamePattern | [`internal/config/customengine.go:18-24`](../../internal/config/customengine.go) |
| 配置侧 secretLiteralPatterns | [`internal/config/customengine.go:30-41`](../../internal/config/customengine.go) |
| resolveEnvRefsStrict 迭代展开 | [`internal/config/customengine.go:397-410`](../../internal/config/customengine.go) |
| 凭据多源 + MaskAPIKey | [`internal/credential/credential.go:91-115`](../../internal/credential/credential.go) |
| 脱敏日志（auth_env 字段名） | [`internal/credential/agent_init.go:281-306`](../../internal/credential/agent_init.go) |
| shellquote POSIX/Windows | [`internal/shellquote/shellquote.go:7-49`](../../internal/shellquote/shellquote.go) |

> 路径说明：本课在 `dev-doc/full/`，源码在仓库根的 `internal/...`，所以相对路径是 `../../internal/...`。

---

## 7. 小结 + 下一课预告

### 本课你必须带走的五句话

1. **报告是流水线末端的「多视图渲染层」**：通过 `Reporter` 策略接口（[`reporter.go:12-14`](../../internal/report/reporter.go)），同一份事实数据（`Input`）被渲染成 JSON（机器事实源）/ JUnit（CI 拍板）/ HTML（人读 + 自包含 + 前端接管）三种视图——零信息冗余，单一真相源。
2. **Anthropic 兼容 = 生态借力**：`grading.json` / `eval_metadata.json` / `benchmark.json` 全部对齐 Anthropic skill-creator 的 schema，让 skill-up 的产物能被 eval-viewer 等官方工具零成本消费。「少发明轮子，多对齐标准」是开源项目进入主流生态的关键。
3. **A/B Benchmark 把 Skill 有效性量化**：`with_skill - without_skill` 的 delta（4 指标 + mean+stddev）回答「这个 Skill 值得上线吗」「结果稳吗」——skill-up 从「跑用例工具」升级成「Skill ROI 评估平台」。
4. **IterationWorkspace 是 Anthropic 兼容目录树的治理者**：`validateCaseID` 三重拦截 + `isRootedPath` Windows 防御 = OS 无关的路径穿越防御；`ensureCaseDirs` 数据驱动布局，配置变了目录自适应。
5. **Custom Engine 是完全不可信边界，用纵深防御堵四类威胁**：路径穿越/TOCTOU（`workspacePath` 每个 use site re-resolve）、凭据泄漏（`maskAPIKey` 字段级贴面 + 凭据不进命令行/URL）、SSRF（不跟随 3xx + scheme 白名单 + size cap）、OOM（多层 size cap 多读 1 字节）。**TOCTOU 的核心认知是「check 挪到离 use 越近越好」，不是「检查更严格」。**

### 30 秒面试口径（背下来）

> 「skill-up 的输出层用策略模式把同一份评测数据渲染成 JSON 事实源 / JUnit CI 视图 / HTML 自包含报告三种格式，并通过适配器对齐 Anthropic 的 grading/eval_metadata/benchmark schema，让本地产物能被 eval-viewer 等官方工具直接消费——这是生态借力。Benchmark 用 with/without A/B 对比，算 mean+stddev + delta，把『Skill 到底有没有用』量化成 pass_rate / time / tokens 四个维度的工程数据。
>
> 安全这一块，Custom Engine 是完全不可信边界——它返回任意 JSON、能在 workspace 里种文件、会 echo env。我用一套纵深防御堵四类威胁：路径穿越和 TOCTOU 靠 `workspacePath` 在每一个 rm/read 前重新解析符号链接（核心是把 check 挪到离 use 越近越好）；凭据靠字段级 `maskAPIKey` 全脱敏 + 渲染后扫描禁止 key 进命令行/URL；SSRF 靠不跟随重定向 + scheme 白名单 + size cap；OOM 靠多层 size cap 多读 1 字节判断。配置侧还有 strict 模式迭代展开防 wrapper 走私。这套设计不靠任何单点，而是每层都假设其他层失效也能挡。」

### 下一课预告：L08 · 面试与实战

L07 把「产出 + 安全」讲完了，L08 是课程的**毕业交付**：

- 把前 7 课的知识点**翻译成简历描述**（按「架构 / 量化 / 安全 / 工程化」四条线组织）；
- 10+ 道高频面试题模拟（覆盖 L01–L07 全部红线）；
- 一个 **Capstone 实战项目**（从 0 写一个 Skill + 评测用例 + A/B 报告 + 安全自检），让你能指着 PR 说「这就是我用 skill-up 做的」。

> L08 之后，这套课程就完成了从「听说过」→「能讲透」→「能落地」的闭环。前 7 课是知识，L08 是转化——把你学到的变成简历上和面试里能打的东西。
